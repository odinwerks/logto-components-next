services:
  # ── logto-dash (Next.js dashboard) ──────────────────────────────────────────
  logto-dash:
    build:
      context: .
      dockerfile: Dockerfile
      # NEXT_PUBLIC_* vars are baked into the client bundle at build time.
      # They are sourced from your .env file when you run `docker compose build`.
      # Set PUBLIC_BASE_URL in .env to your public CF tunnel URL.
      args:
        NEXT_PUBLIC_THEME:              ${THEME}
        NEXT_PUBLIC_LANG_MAIN:          ${LANG_MAIN}
        NEXT_PUBLIC_LANG_AVAILABLE:     ${LANG_AVAILABLE}
        NEXT_PUBLIC_MFA_ISSUER:         ${MFA_ISSUER}
        NEXT_PUBLIC_DEFAULT_THEME_MODE: ${DEFAULT_THEME_MODE}
        NEXT_PUBLIC_USER_SHAPE:         ${USER_SHAPE}
        NEXT_PUBLIC_NAME_TYPE:          ${NAME_TYPE}
        NEXT_PUBLIC_LOAD_TABS:          ${LOAD_TABS}
        NEXT_PUBLIC_DELETE_REDIRECT_DELAY: ${DELETE_REDIRECT_DELAY}
        NEXT_PUBLIC_BACKEND_TYPE:       ${BACKEND_TYPE}
        NEXT_PUBLIC_SIGNOUT_REDIRECT_DELAY: ${SIGNOUT_REDIRECT_DELAY}
        NEXT_PUBLIC_FORCE_ANIMATIONS:   ${NEXT_PUBLIC_FORCE_ANIMATIONS}
    networks:
      - dash-net
    # The entrypoint reads this secret and builds the authenticated REDIS_URL at
    # runtime; the password is never part of Compose's service environment.
    secrets:
      - redis_password
    # Port 2999 is NOT exposed to the host - only cloudflared can reach it
    # via the internal dash-net network. Remove the comment below if you need
    # direct host access for debugging:
    # ports:
    #   - "2999:2999"
    environment:
      - NODE_ENV=production
      # ── Core Logto config ─────────────────────────────────────────────────
      - APP_ID=${APP_ID:?APP_ID is required - set it in .env}
      - APP_SECRET=${APP_SECRET:?APP_SECRET is required - set it in .env}
      - ENDPOINT=${ENDPOINT:?ENDPOINT is required - set it in .env}
      - BASE_URL=${PUBLIC_BASE_URL}
      - COOKIE_SECRET=${COOKIE_SECRET:?COOKIE_SECRET is required - set it in .env}
      - SCOPES=${SCOPES:?SCOPES is required - set it in .env}
      # ── M2M (account deletion, RBAC) ─────────────────────────────────────
      - LOGTO_M2M_APP_ID=${LOGTO_M2M_APP_ID}
      - LOGTO_M2M_APP_SECRET=${LOGTO_M2M_APP_SECRET}
      - LOGTO_INTROSPECTION_URL=${LOGTO_INTROSPECTION_URL}
      - LOGTO_M2M_RESOURCE=${LOGTO_M2M_RESOURCE}
      # ── Backend and phone-country behavior gates ─────────────────────────
      - BACKEND_TYPE=${BACKEND_TYPE}
      - COUNTRY_CODE_ALLOW_LIST=${COUNTRY_CODE_ALLOW_LIST}
      - COUNTRY_CODE_BLOCK_LIST=${COUNTRY_CODE_BLOCK_LIST}
      # ── Avatar storage (S3-compatible / Supabase) ─────────────────────────
      - PFP_BACKEND=${PFP_BACKEND}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME}
      - S3_PUBLIC_URL=${S3_PUBLIC_URL}
      - S3_ENDPOINT=${S3_ENDPOINT}
      - S3_ACCESS_KEY_ID=${S3_ACCESS_KEY_ID}
      - S3_SECRET_ACCESS_KEY=${S3_SECRET_ACCESS_KEY}
      - S3_REGION=${S3_REGION}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      # ── Debug (set DEBUG=true to enable verbose server logging) ───────────
      - DEBUG=${DEBUG}
      # ── UI / theme vars (server-side reads bare names via readEnv) ──────────
      - THEME=${THEME}
      - LANG_MAIN=${LANG_MAIN}
      - LANG_AVAILABLE=${LANG_AVAILABLE}
      - MFA_ISSUER=${MFA_ISSUER}
      - DEFAULT_THEME_MODE=${DEFAULT_THEME_MODE}
      - USER_SHAPE=${USER_SHAPE}
      - NAME_TYPE=${NAME_TYPE}
      - LOAD_TABS=${LOAD_TABS}
      - DELETE_REDIRECT_DELAY=${DELETE_REDIRECT_DELAY}
      # ── Logging ───────────────────────────────────────────────
      - LOG_BACKEND=${LOG_BACKEND}
      - LOG_LEVEL=${LOG_LEVEL}
      - LOGGING_WEBHOOK_URL=${LOGGING_WEBHOOK_URL}
      # ── Fallback URLs ──────────────────────────────────────────
      - APP_URL=${APP_URL}
      # ── Redis (distributed rate limiting / locking / token cache) ────────────
      # docker-entrypoint.sh adds the mounted password at runtime.
      - REDIS_URL=redis://redis:6379
      # ── Sign-out redirect delay (server-side) ──────────────────
      - SIGNOUT_REDIRECT_DELAY=${SIGNOUT_REDIRECT_DELAY}
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('net').createConnection(2999,'localhost').on('connect',function(){process.exit(0)}).on('error',function(){process.exit(1)})\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    restart: unless-stopped

  # ── Cloudflare Tunnel ────────────────────────────────────────────────────────
  # Routes traffic from your public CF tunnel URL to logto-dash:2999 internally.
  # Configure the tunnel routing in the Cloudflare dashboard:
  #   Public hostname → http://logto-dash:2999
  cloudflared:
    # Review the tag and registry digest periodically; update only after verifying it via Docker Registry API.
    image: cloudflare/cloudflared:latest@sha256:e39ee8da81ad5e05d77f38d2f51c60ca51bf2a8450ac3abab50c17fdb91d91bf
    command: tunnel --no-autoupdate run
    networks:
      - dash-net
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      logto-dash:
        condition: service_healthy
    restart: unless-stopped

  # ── Redis (optional) ─────────────────────────────────────────────────────────
  # Only needed when REDIS_URL is set. Provides distributed rate limiting,
  # locking, and token caching via app/lib/distributed-state.ts.
  # Remove or comment out this service if you are not using Redis.
  redis:
    image: redis:7-alpine
    container_name: logto-dash-redis
    # Read the password from a mounted Compose secret rather than embedding it
    # in Config.Cmd. The shell only passes the generated config over stdin.
    command: ["sh", "-c", "printf 'requirepass %s\\n' \"$$(cat /run/secrets/redis_password)\" | exec redis-server -"]
    networks:
      - dash-net
    secrets:
      - redis_password
    ports:
      - "127.0.0.1:2998:6379"
    restart: unless-stopped

volumes: {}

networks:
  dash-net:
    driver: bridge

secrets:
  redis_password:
    environment: REDIS_PASSWORD
